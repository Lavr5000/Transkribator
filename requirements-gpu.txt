# WhisperTyping - GPU Accelerated Version
# For NVIDIA GPUs with CUDA support
# Install with: pip install -r requirements-gpu.txt

# All base requirements
-r requirements.txt

# PyTorch with CUDA
torch>=2.0.0
torchaudio>=2.0.0

# CUDA libraries (for faster-whisper)
nvidia-cudnn-cu12>=8.9.0
nvidia-cublas-cu12>=12.1.0

# CTranslate2 for optimized inference
ctranslate2>=4.0.0
